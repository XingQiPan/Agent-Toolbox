{
  "name": "@agent-toolbox/api",
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@agent-toolbox/core": "workspace:*",
    "@agent-toolbox/plugin-json-basic": "workspace:*",
    "fastify": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
